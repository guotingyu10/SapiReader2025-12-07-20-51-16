为项目添加优化拼音阅读时的发音效果,在
"EscapeRules":中设置汉语拼音
并将每个拼音转为,可以正常发音的汉字.
"a":"啊",
"ai":"爱",
"an":"安",
在朗读拼音ai时,由于a绑定了读音"啊",ai本应读为"爱",但读成了啊i,优化替换逻辑解决此问题.
采用“长度优先”或“最长匹配扫描”即可从根本上避免该类冲突。
优化项目修改config.json的体验,将config.json设置为可以右键选择用其他软件打开,并且其他软件编辑保存后,自动重启,并采用新配置.


.\.venv\Scripts\qwen-tts-demo ..\Qwen3-TTS-12Hz-0.6B-CustomVoice --device cpu --dtype float32 --no-flash-attn --ip 127.0.0.1 --port 8000

2026.3.19-4.54.4.png11111222







运行指令:
python --version
python -m pip --version

conda --version

conda create -n qwen3-tts python=3.12 -y

nvidia-smi

conda install -n qwen3-tts -c pytorch -c nvidia pytorch torchaudio pytorch-cuda=12.4 -y

conda run -n qwen3-tts python -c "import torch, qwen_tts; print('torch', torch.__version__, 'cuda', torch.version.cuda, 'cuda_available', torch.cuda.is_available())"
conda run -n qwen3-tts qwen-tts-demo --help


conda install -n qwen3-tts -c conda-forge sox -y


conda run -n qwen3-tts sox --version
conda run -n qwen3-tts python -c "import torch; print('torch', torch.__version__, 'cuda', torch.version.cuda, 'cuda_available', torch.cuda.is_available(), 'gpu', torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)"
conda run -n qwen3-tts qwen-tts-demo --help


conda run -n qwen3-tts qwen-tts-demo Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice --ip 127.0.0.1 --port 8000 --device cuda:0 --dtype float16 --no-flash-attn

try { (Invoke-WebRequest -TimeoutSec 2 http://127.0.0.1:8000/).StatusCode } catch { $_.Exception.Message }try { (Invoke-WebRequest -TimeoutSec 2 http://127.0.0.1:8000/).StatusCode } catch { $_.Exception.Message }



Start-Sleep -Seconds 45
try { (Invoke-WebRequest -TimeoutSec 5 http://127.0.0.1:8000/).StatusCode } catch { $_.Exception.Message }



Get-Process python -ErrorAction SilentlyContinue | Sort-Object StartTime -Descending | Select-Object -First 5 Id,ProcessName,CPU,StartTime


Get-NetTCPConnection -LocalPort 8000 -ErrorAction SilentlyContinue | Select-Object -First 5 LocalAddress,LocalPort,State,OwningProcess


